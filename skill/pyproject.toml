[project]
name = "ocr-enhanced-code-review"
version = "0.1.0"
description = "Deterministic OCR preflight helpers for Codex code review"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
validation = [
  "pyyaml>=6.0,<7",
  "pytest>=8,<9",
  "ruff>=0.16.8,<0.17",
]

[tool.uv]
package = false
